# Data converter

*This program lets user to calculate data quantities (bytes, kilobytes, megabytes).
* User can calculate how much bytes are in megabytes and so on. 
* User also can see how much data has his computer files. 
